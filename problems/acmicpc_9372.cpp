#include <stdio.h>
int main(){
    int t;
    scanf("%d",&t);
    while(t--){
        int n,x;
        scanf("%d%d",&n,&x);
        for(int i=0;i<x;i++){
            int a,b;
            scanf("%d%d",&a,&b);
        }
        printf("%d\n",n-1);
    }
}